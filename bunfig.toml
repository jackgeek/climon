[test]
# Force the default CLIMON_HOME onto a throwaway temp dir so a test that forgets
# to isolate can never touch a developer's real ~/.climon. See the preload file.
preload = ["./tests/preload-isolate-home.ts"]
