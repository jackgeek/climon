//! Default loopback port the ingest daemon listens on. 1:1 port of
//! `src/remote/ingest-port.ts`.

/// Default loopback port the ingest daemon listens on.
pub const DEFAULT_INGEST_PORT: u16 = 3132;
