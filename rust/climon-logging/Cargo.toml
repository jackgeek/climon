[package]
name = "climon-logging"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "climon structured logging, levels, redaction, pretty output, and sinks (Rust port of the TS client)"

[dependencies]
serde_json = { workspace = true }
